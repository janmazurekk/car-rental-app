@using CarRentalACC.Data

<CascadingAuthenticationState>
    <Router AppAssembly="@typeof(App).Assembly">
        <Found Context="routeData">
            <AuthorizeRouteView RouteData="@routeData" DefaultLayout="@typeof(MainLayout)" />
            <FocusOnNavigate RouteData="@routeData" Selector="h1" />
        </Found>
        <NotFound>
            <PageTitle>Not found</PageTitle>
            <LayoutView Layout="@typeof(MainLayout)">
                <p role="alert">Sorry, there's nothing at this address.</p>
            </LayoutView>
        </NotFound>
    </Router>
</CascadingAuthenticationState>

@*@code {
    [Inject] ICarsService carsService { get; set; } = default;

    protected override void OnAfterRender(bool firstRender)
    {
        if (firstRender)
        {
            carsService.AddCar("BMW", "szybkie", 500);
            carsService.AddCar("Mercedes", "wygodne", 800);
            carsService.AddCar("Ferrari", "ultra szybkie", 1500);
            carsService.AddCar("Maybach", "luksusowe", 3000);
            carsService.AddCar("Opel", "ekonomiczne", 200);
        }
    }
}*@